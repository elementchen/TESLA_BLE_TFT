# 🚗 特斯拉仪表盘假数据流“剧本”时间轴设计规范

本剧本设计了 39 秒（每秒 20 帧，共 780 帧）的完整车主一日用车故事线。
通过连续的动态假数据流，无缝测试并展示 **开门提示界面**、**驾驶主界面**、以及 **充电进度界面** 的流畅转换与精美排版。

---

## 📅 剧本故事线场景时间轴

| 时间段 (秒) | 帧区间 | 车辆状态 | HMI 展示界面 | 遥测假数据表现细节 |
| :--- | :--- | :--- | :--- | :--- |
| **0.0 - 5.0** (5s) | 0 - 99 | 解锁与上车 | **开门界面** | 车辆解锁 (unlocked)。大尺寸极简特斯拉车身，主驾驶门 (FL) 和左后门 (RL) 开启。电量 62.0%，ODO 68420 km。 |
| **5.0 - 8.0** (3s) | 100 - 159 | 准备起步 | **驾驶界面 (P档)** | 所有车门关闭。自动切回驾驶页面。中央显示巨型红色 **`P`** 特写。胎压冷态 2.3 bar，车内降温开启，电机功率 `1.5 kW`。 |
| **8.0 - 12.0** (4s) | 160 - 239 | D档轻油门 | **驾驶界面 (D档)** | 挂 D 档，前 2 秒中央大字特写红色 **`D`**；后 2 秒切换为时速数值（从 0 滑增至 30 km/h），左侧带小红字 `D`。功率条微红输出 `+12.0 kW`。 |
| **12.0 - 18.0** (6s) | 240 - 359 | 强加速超车 | **驾驶界面 (D档)** | 车速从 30 km/h 急升至 120 km/h，功率条红色大面积拉伸 `+85.0 kW`。高速行驶中四个胎压摩擦升温爬升至 `2.6 bar`。车温降至 `22.0 C`。 |
| **18.0 - 23.0** (5s) | 360 - 459 | 动能回收与刹车 | **驾驶界面 (动能回收)**| 时速从 120 km/h 回落至 10 km/h。功率能耗条填充**亮绿色向左收缩**（动能回收峰值达到 `-60.0 kW`），电池 SOC 充回 62.1%。 |
| **23.0 - 27.0** (4s) | 460 - 539 | R档倒车入库 | **驾驶界面 (R档)** | 挂 R 档，前 2 秒中央大字特写红色 **`R`**；后 2 秒显示倒车速度 `5 km/h`。功率条微弱功率变动。 |
| **27.0 - 31.0** (4s) | 540 - 619 | 突发状况：门被打开 | **开门警示界面** | 车辆停稳回 P 档并解锁。在准备熄火下车时，**主驾驶门 (FL) 突然被推开**。屏幕一瞬间强制切回 **开门界面**。车身红色开门位图展示，右侧 1-bit 小字警示 `"FL DOOR OPEN"`。 |
| **31.0 - 39.0** (8s) | 620 - 779 | 插枪充电中 | **充电界面** | 车门关好，锁车 (locked)。随后检测到插枪充电，自动切入**充电界面**：电量直槽从 62.1% 缓慢前展，显示充电功率 `7.2 kW`，剩余充电时间 `3h 45m`。 |

---

## 🛠 数据帧结构定义 (`DashData`)

我们在程序里模拟生成的每一帧数据将严格符合车机蓝牙的 `DashData` C structure 定义，保证剧本测试完成后，能一键无缝还原到车机蓝牙通路。

```cpp
struct DashData {
    float speed_kmh = 0.0f;
    char  gear = 'P';       // 'P', 'R', 'N', 'D', 'S'
    uint32_t odometer_km = 0;
    bool valid = false;
    bool vehicle_awake = false;
    bool ble_connected = false;

    // ─── 扩展遥测数据段 ───
    float motor_power_kw = 0.0f;     // 电机功率 (负值为回收，正值为输出)

    // 充电数据
    bool charging = false;           // 连接充电枪且在充电中
    float charge_power_kw = 0.0f;    // 充电实时功率 (kW)
    int32_t charge_limit_soc = 80;   // 充电限制上限 (如 80%)
    int32_t minutes_to_charge_limit = 0; // 充电剩余时间 (分钟)
    std::string charging_state_str = "Disconnected";

    // 电池与估计续航
    int32_t battery_level = 0;       // 剩余电量 SOC (%)
    float battery_range_km = 0.0f;   // 估计续航距离 (KM)

    // 客舱与外界环境温度
    float inside_temp = 0.0f;        // 车内温度 (℃)
    float outside_temp = 0.0f;       // 车外温度 (℃)

    // 胎压状态 (bar)
    float tpms_fl = 0.0f;            // 前左胎压
    float tpms_fr = 0.0f;            // 前右胎压
    float tpms_rl = 0.0f;            // 后左胎压
    float tpms_rr = 0.0f;            // 后右胎压

    // 车身开口与中控锁定
    bool door_open_fl = false;       // 主驾驶门 (前左)
    bool door_open_fr = false;       // 副驾驶门 (前右)
    bool door_open_rl = false;       // 后排左门 (后左)
    bool door_open_rr = false;       // 后排右门 (后右)
    bool door_open_trunk_front = false; // 前备箱 (Frunk)
    bool door_open_trunk_rear = false;  // 后备箱 (Trunk)
    bool locked = true;              // 整车上锁状态
};
```

---

## 🎨 界面渲染派发机制与局部离屏缓冲

每帧数据注入后，`Display::render_dashboard` 将自动根据状态判定，跳转到相应的 HMI 渲染逻辑。

为了保证所有界面切换零残影、文本变化 100% 毫无拉帘扫描线，界面由三块完全隔离的条状离屏双缓冲画布在内存中渲染并批量 DMA 送出：
1. **时速与挡位画布**：`260x80` 像素
2. **双色功率条画布**：`260x26` 像素
3. **下部卡片组画布**：`320x32` 像素
这不仅消除了分步清空导致的边缘脏像素与拉帘现象，还在极低内存（74KB SRAM）下达成了媲美高档车机的平滑极致画质。
